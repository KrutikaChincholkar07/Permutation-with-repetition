# Permutation-with-repetition
Permutation with repetition is used when each element can be chosen more than once.  The formula for permutation with repetition is:  n r  Where:  n = number of different items r = number of positions to fill
