# Permutation with repetition program

n = int(input("Enter number of items (n): "))
r = int(input("Enter number of positions (r): "))

result = n ** r

print("Permutation with repetition =", result)
